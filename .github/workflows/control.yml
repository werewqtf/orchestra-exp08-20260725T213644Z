name: exp08-control
on:
  pull_request:
permissions:
  contents: read
jobs:
  legacy-gate:
    if: contains(github.event.pull_request.labels.*.name, 'run-ci')
    runs-on: ubuntu-latest
    steps:
      - run: echo "control gate executed"
